#[derive(Debug, PartialEq, Eq, Hash, Clone)]
pub enum Id {
    GlobalListener,
    Help,
    ScoreTable,
    UsernameInput,
    SchemaView,
    Timer,
    Score,
    Editor,
    Result,
    Question,
}
